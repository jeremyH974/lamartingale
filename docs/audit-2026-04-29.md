# Audit Sillon — 29 avril 2026

## TL;DR (5 lignes max)

Repo **techniquement sain** (715/715 tests, master = `fbf910c` intact, branche Scénario B intacte) mais **le brief Phase Alpha sous-estime 3 zones critiques** : (1) build TS strict est passé de 13 → 14 erreurs depuis la baseline, (2) 950 épisodes sur 5 tenants sont **toujours sans embeddings** (R4 latente), (3) le hub master prod **n'expose AUCUNE mention légale / RGPD / cookies** alors que tous les frontends mentionnent "IA" et trackent côté serveur. Bonnes surprises : `/api/cross/search` est déjà publiquement exposé (mais sans UI), 64 briefs en DB (cohérent), isolation tenant clean. Mauvaises surprises : ms-hub-v2-preview est en 401 (AUTH_BYPASS_PREVIEW vraisemblablement déjà retiré, donc preview vérifiable via Vercel CLI seulement), `/api/cross/search` répond en 5107 ms (lent), tag `scenario-b-v1-cloture` est annoté donc `git rev-parse` retourne le SHA tag (62dff59) pas le commit (634bfeb) — piège pour scripts de vérif.

## Axe 1 — État technique

### Tests
- **715/715 passants** (50 fichiers, durée 5.32s) ✓ aligné cible.
- 0 test skipped, 0 todo.

### Build TS strict
- **14 erreurs** (+1 vs baseline 13 mémorisée le 27/04).
- Détail :
  - `engine/__tests__/cross-reference-episode.test.ts` : 2 erreurs (TS2493, TS18048) — pré-existantes.
  - `engine/__tests__/transcribe-audio.test.ts` : 11 erreurs (TS2493, TS18048) — pré-existantes.
  - **`engine/__tests__/output-formatters.test.ts:131`** : 1 erreur **NOUVELLE** — `Buffer<ArrayBufferLike>` incompatible avec `Buffer` (typage Node 24 / `Symbol.toStringTag`). Apparue depuis le 27/04. À enregistrer dans `docs/debt-tracking.md`.
- `npm test` reste vert : tests fonctionnels OK, ces erreurs sont du typage strict pur.

### Dépendances (npm audit prod)
- **3 vulnérabilités modérées** (chaîne `uuid<14 → svix → resend`).
- Aucune critique/haute.
- Fix dispo via breaking change `resend@6.1.3` (`npm audit fix --force`).

### Outdated (top)
- `@anthropic-ai/sdk 0.89.0 → 0.91.1` (mineur)
- `@neondatabase/serverless 1.0.2 → 1.1.0`
- `vitest 4.1.4 → 4.1.5`
- `typescript 6.0.2 → 6.0.3`
- Aucun majeur en retard.

### Architecture confirmée
- `engine/` (api, db, ai, cache, cross, scraping, config, output, pipelines, video, auth, __tests__)
- `instances/` : **13 fichiers config** (vs 7 actifs en prod selon CLAUDE.md). Inactifs : `allolamartingale`, `demainvousappartient`, `fleurons`, `iftd`, `onlacherien`. Présents en DB (cf. axe 2).
- `frontend/` : 7 HTML (`v2`, `episode`, `hub`, `v2-dashboard`, `guest-brief`, `login`, `login-soon`)
- `vercel-configs/` : 7 configs (lamartingale, gdiy, hub, lepanier, finscale, passionpatrimoine, combiencagagne) — pas de config Vercel pour les 5 tenants "inactifs" malgré les 1 058 épisodes en DB.
- `cli/`, `scripts/`, `api/` présents.

### Git
- Working tree **clean**.
- Branche courante : `feat/hub-v2-scenario-b @ 634bfeb` (13 commits ahead de master, 0 derrière).
- Tag annoté `scenario-b-v1-cloture` : SHA-tag `62dff59`, **pointe bien sur commit `634bfeb`** (déréférencement via `^{commit}`). ⚠ Piège : `git rev-parse <tag>` sans suffixe retourne le SHA-tag, pas le commit — scripts de vérif doivent utiliser `^{commit}`.
- Master `fbf910c` ✓ intact, dernier commit "Merge Phase L — fix Bug #6 badge contraste WCAG".
- Branches locales : `master`, `feat/hub-v2-scenario-b`, `feat/video-pipeline` (`a3a91c1`, vidéo Phase 7b non-mergée, dette mémorisée).
- Branches remote : `master`, `feat/hub-v2-scenario-b`, `fix/hub-ui-phase-a`, `pilot-simulation-inoxtag`, `recon/beta-lamartingale-2026-04-26`.

### Vercel (vérif limitée à `vercel-configs/*.json` + `.vercel/project.json`)
- Working dir lié au projet **`ms-hub`** (org `team_js9AR0tn9uWRKIO2sNHul9u7`).
- 7 configs cohérentes ; rewrites OK pour chaque tenant.
- ⚠ **Doute non-vérifié (vercel CLI skip)** : `AUTH_BYPASS_PREVIEW=true` mémorisé sur Preview de `ms-hub`, mais `https://ms-hub-v2-preview.vercel.app/` retourne **401**. Probable que la variable a été retirée OU SSO Vercel restreint l'accès. À confirmer par le user dans le dashboard Vercel.

## Axe 2 — État produit

### Capacités exposées (vérification publique)

| Capacité | Statut | Lien public / fichier |
|---|---|---|
| Hub v1 prod (master) | ✅ Live | `https://ms-hub.vercel.app/` — 3 sections seulement (Podcasts, Invités partagés, Refs) |
| Hub v2 Preview | ⚠ 401 | `https://ms-hub-v2-preview.vercel.app/` — auth-gated (cf. doute Vercel) |
| `/api/cross/guests/shared` | ✅ Public | Renvoie 72 invités avec `appearances[]`, `is_cross_podcast`, `total_episodes` |
| `/api/cross/search` (R1) | ✅ **Public** (UI absente) | Réponse JSON, 5107 ms (lent) — **exposé sans UI** |
| `/api/universe` | ⚠ 401 | Sans cookie session — utilisé via cookie côté hub |
| Site individuel LM (`lamartingale-v2.vercel.app`) | ✅ Live | Mentionne "Recherche sémantique IA", "Quiz adaptatif IA", "Assistant" — **0 mention privacy/RGPD/cookies/CGU** |
| Page guest-brief individuelle | ✅ Live (rewrite Vercel) | `/guest-brief/eric-larcheveque` route OK dans `vercel-hub.json` |
| RAG showcase passif | ⚠ Hub v2 only | `frontend/data/showcase-rag-responses.json` (Scénario B M2) |
| Quiz quality badge | ⚠ Pas vu visuellement | À confirmer via preview start |
| Briefs invités | ✅ DB + UI v2 | **64 briefs DB** sur 1 261 cpg ; sur top 10 multi-pod : 8/10 ont brief, 2/10 KO (`pierre marin`, `ilan abehassera`) |
| Privacy notice / Mentions légales / Bandeau cookies / CGU | ❌ **ABSENT** sur hub master ET sur sites individuels |

### Capacités codées mais NON exposées (top 10)

| # | Capacité | Statut tech | Effort exposition |
|---|---|---|---|
| 1 | UI **R1 recherche cross-podcast** (`/api/cross/search`) | Endpoint **déjà public**, UI nulle part | 3-4 h CC, $0 LLM |
| 2 | UI **R2 chat conversationnel** (`POST /api/cross/chat`) | Endpoint code OK, UI nulle part | 4-6 h CC, $0 LLM |
| 3 | **R3 page pack pilote Boissenot LM #174** | Pack en sandbox `experiments/.../pack-pilote-stefani-orso-v3-final/` (gitignored), **pas de page publique** | 6-8 h CC, $0 |
| 4 | **R4 embeddings 950 ép.** absents (iftd 706, dva 98, onlacherien 82, allolamartingale 58, fleurons 6) | Pipeline `embeddings.ts` OK, jamais run sur ces tenants | 1-2 h, ~$5 LLM |
| 5 | **R5 11 briefs cpg complémentaires** (75 multi-pod − 64 actuels) | `persistGuestBrief` OK, dette ~$0,40 | 2 h, ~$0,40 |
| 6 | **5 livrables Sillon** (key-moments / quotes / cross-refs by lens / newsletter / brief-annexe) | 4 packs pilote 12/12 ≥ 7,5/10 ; **`runPack()` throw not implemented** | 2-3 jours + ~$1 800 |
| 7 | **`/api/cross/timeline`**, **`/api/cross/sponsors`**, **`/api/cross/analytics`** | Endpoints actifs, UI nulle part | 1 h chacun |
| 8 | **`/api/links/stats`** (top outils/entreprises) | Actif, UI nulle part | 2-3 h |
| 9 | **`/api/quiz`** côté hub | Actif sur sites individuels, **non exposé sur hub** | 4-6 h |
| 10 | Vidéo pipeline (Phase 7b) | Branche `feat/video-pipeline @ a3a91c1` non-mergée — dette KO depuis Phase 8.4 | Rebase 1-2 jours |

### Volumétrie produit (DB Neon, vérifiée)

- **3 354 épisodes** sur 11 tenants (cohérent mémoire).
- gdiy 963 / iftd 706 / lepanier 506 / finscale 338 / lamartingale 294 / passionpatrimoine 198 / combiencagagne 105 / dva 98 / onlacherien 82 / allolamartingale 58 / fleurons 6.
- **1 261 cross_podcast_guests**, **75 multi-pod**, **64 briefs** ✓ aligné.
- **Embeddings** : 2 355 sur 3 354 (70 %) — **5 tenants à 0 % embeddings (950 eps)**.
- **Cross_refs JSONB** : 100 % sauf finscale **8/338 = 2 %** (anomalie isolée — bug ingest ?).
- **editorial_events** : 0 rows ✓ aligné mémoire (pipeline 5 livrables n'écrit pas en DB).
- **Quiz_questions** : 5 806 sur 6 tenants (cohérent), **0 quiz pour iftd/dva/onlacherien/allolamartingale/fleurons**.
- **Articles riches** (>500c) : 710 ✓ aligné, ultra-concentré sur **gdiy (468) + lamartingale (242)**.
- **Guests par tenant** avec linkedin : LM 100 % (223/223) ; 4 tenants à **0 %** linkedin (dva/iftd/onlacherien/allolamartingale).

## Axe 3 — Audit factuel post-Scénario B

| Vérification | Attendu | Constaté | OK |
|---|---|---|---|
| Branche `feat/hub-v2-scenario-b` @ 634bfeb intacte | oui | `634bfebe38d89...` | ✓ |
| Tag `scenario-b-v1-cloture` → 634bfeb | oui | `^{commit}` = `634bfeb` ✓ (mais SHA-tag = `62dff59`) | ✓ |
| Master `fbf910c` intact | oui | `fbf910cfe612...`, 0 nouveau commit depuis 28/04 | ✓ |
| Working tree clean | oui | clean | ✓ |
| Preview `ms-hub-v2-preview.vercel.app` actif | oui | **401** | ⚠ |
| 64 briefs cpg DB | oui | 64 ✓ | ✓ |
| 75 multi-pod | oui | 75 ✓ | ✓ |
| 1 261 cpg total | oui | 1 261 ✓ | ✓ |
| 3 354 épisodes | oui | 3 354 ✓ | ✓ |
| 11 tenants | oui | 11 ✓ | ✓ |
| `editorial_events` 0 rows | oui | 0 ✓ | ✓ |
| Isolation tenant cross-similarité | oui | `episode_similarities` a `tenant_id` directement (pas besoin de join) | ✓ |
| Mémoire CC `project_scenario_b_cloture_2026_04_28.md` | oui | 89 lignes lisibles, à jour | ✓ |
| Mémoire CC `reference_audit_capabilities_2026_04_28.md` | oui | 76 lignes lisibles, à jour | ✓ |
| `MEMORY.md` index | oui | 24 lignes, références correctes | ✓ |

## Écarts vs hypothèses du brief Phase Alpha

1. **Tag scenario-b-v1-cloture** : la mémoire CC dit "tag → 634bfeb" mais `git rev-parse <tag>` retourne le SHA du **tag annoté** (`62dff59`). Tout script de vérif Phase Alpha doit utiliser `<tag>^{commit}`. **Impact** : risque de fail bénin sur scripts de pre-flight check.
2. **Preview Vercel inaccessible publiquement (401)** : le brief suppose `ms-hub-v2-preview.vercel.app` accessible. AUTH_BYPASS_PREVIEW vraisemblablement retiré (ou jamais activé sur cette URL alias). **Impact** : aucune démo publique exploitable Phase Alpha sans rebascule manuelle ou nouvelle preview.
3. **Build TS strict +1 erreur** (`output-formatters.test.ts:131`) apparue post-baseline 27/04. **Impact** : si la Phase Alpha intègre un job CI build strict, il échouera sur 14 erreurs au lieu de 13.
4. **`/api/cross/search` est déjà public** sans UI ni rate-limiting : la mémoire dit "UI absente" mais ne mentionne pas que l'endpoint répond aux requêtes anonymes. **Impact** : risque vol IP par scraping concurrents avant même expo formelle ; à protéger ou rate-limiter avant d'ajouter une UI.
5. **`/api/cross/search` répond en 5107 ms** sur production (jeu test "intelligence artificielle"). **Impact** : performance UX à valider/optimiser avant exposition R1.
6. **5 tenants à 0 % embeddings** (950 eps) plutôt que les "999" indiqués mémoire — l'écart est mineur (chiffres mémoire imprécis), mais le scope R4 est confirmé : **iftd, dva, onlacherien, allolamartingale, fleurons** intégralement vides. **Impact** : ces 5 tenants sont absents de toute recherche sémantique cross-podcast.
7. **Anomalie finscale `cross_refs`** : seulement 8/338 épisodes ont un `cross_refs` JSONB peuplé alors que tous les autres tenants sont à ~100 %. **Impact** : graph cross-refs sous-représente finscale ; bug ingest probable à investiguer.
8. **0 mention légale/RGPD/cookies sur master prod** alors que tous les sites mentionnent "IA" et que des cookies de session sont posés (auth magic-link Resend). **Impact** : conformité RGPD à statuer **avant tout pilote envoyable** Stefani / Orso.
9. **5 tenants ont des configs `instances/*.config.ts`** (allolamartingale/dva/fleurons/iftd/onlacherien) **mais pas de projet Vercel**, alors qu'ils contiennent 1 058 épisodes en DB. **Impact** : ces tenants sont indexés mais non navigables côté frontend public.
10. **3 vulnérabilités prod modérées** (uuid/svix/resend) : non bloquantes mais à patcher si Phase Alpha touche à `resend` (auth magic-link).

## Bugs détectés (sans les fixer)

1. **TS2345 nouveau dans `output-formatters.test.ts:131`** — Buffer typing Node 24. **Criticité** : faible (test fonctionnel). **Scope** : 1 ligne. **Fix estimé** : 10 min.
2. **`finscale.cross_refs` peuplé à 2 %** (8/338 vs ~100 % ailleurs). **Criticité** : moyenne (graph cross-refs incomplet). **Scope** : pipeline ingest finscale. **Fix estimé** : 1-2 h investigation + reprocess.
3. **Mentions légales / RGPD / cookies absents** sur tout le footprint public. **Criticité** : haute (conformité, surtout si pilote Stefani envoyable). **Scope** : footers de 7 frontends + page dédiée. **Fix estimé** : 4-6 h (rédaction + intégration).
4. **`/api/cross/search` non rate-limité, 5107 ms, exposé public** sans UI. **Criticité** : moyenne (vol IP + DoS facile). **Scope** : `engine/api.ts` route handler. **Fix estimé** : 2-3 h (rate-limit IP/origin + tuning pgvector ou cache).
5. **AUTH_BYPASS_PREVIEW probablement retiré sans MAJ mémoire CC** (mémoire dit "actif"). **Criticité** : faible (sécurité). **Scope** : doc + Vercel env. **Fix estimé** : 5 min vérif user.
6. **`engine/pipelines/runPack.ts` throw `not implemented`** mais 4 packs pilote 12/12 produits via `experiments/.../phase6-runner.ts`. **Criticité** : haute si réplication Sillon prévue. **Scope** : orchestrateur. **Fix estimé** : 2-3 jours + ~$1 800 LLM (refacto + qualité reproductible).
7. **5 tenants 0 embeddings + 0 quiz** (iftd/dva/onlacherien/allolamartingale/fleurons) : invisibles pour toute recherche/quiz cross. **Criticité** : moyenne (sous-exploitation 1 058 eps). **Scope** : `embeddings.ts` + `generate-quiz.ts`. **Fix estimé** : 2-3 h + ~$5 LLM.
8. **4 tenants 0 linkedin guests** (dva/iftd/onlacherien/allolamartingale) : 162 invités sans linkedin. **Criticité** : faible (qualité brief). **Scope** : pipeline scrape + match-guests. **Fix estimé** : audit nécessaire avant chiffrage.

## Recommandations pour ajuster le brief Phase Alpha

1. **Ajouter pré-flight check explicite** : `git rev-parse scenario-b-v1-cloture^{commit} === 634bfeb...` (pas `git rev-parse scenario-b-v1-cloture`). Vérifier aussi `master = fbf910c` et working tree clean. Une seule étape, mais elle évite faux positifs.
2. **Trancher le bloc conformité (mentions légales / RGPD / cookies) en début de Phase Alpha** : si Phase Alpha vise un pilote envoyable Stefani, c'est P0 préalable. Estim 4-6 h. À sortir de la backlog "post-démo" et remonter en pré-requis.
3. **Cadrer R4 embeddings AVANT R1/R2/R3** : exposer une recherche sémantique sans embeddings sur 5 tenants (1 058 eps non-cherchables) creuse l'écart honnêteté/promesse. Bloc 2-3 h + $5 LLM, à faire en premier.
4. **Sécuriser `/api/cross/search` en parallèle de l'expo R1** : rate-limit + auth-gate "preview pilote" mentionné dans mémoire risques. Sinon expo UI = expo IP gratuite. Petit bloc tech (2-3 h) à intégrer dans R1 plutôt qu'à reporter.
5. **Retirer ou rebrancher `AUTH_BYPASS_PREVIEW`** : status à clarifier (mémoire dit actif, prod renvoie 401). Si Phase Alpha utilise le preview hub v2 pour démo, il faut une URL de démo qui marche. 5 min de check Vercel par le user. À inclure en pré-flight.

## Coût LLM consommé

- **DB queries** : 0 $ (Neon, SELECT only).
- **WebFetch** (5 appels publics) : facturation côté Anthropic harness (estim < 0,05 $).
- **Lectures fichiers + grep** : 0 $.
- **TOTAL audit** : **< 0,05 $** sur cap 2 $.
- Aucune génération IA déclenchée (cap respecté largement).

---

## Fin d'audit

- Working tree toujours clean (vérifié post-audit, aucune modif repo).
- Script DB temporaire à supprimer : `C:\Users\jerem\AppData\Local\Temp\sillon-audit\db-audit.ts`.
- Rapport déposé : `C:\Users\jerem\AppData\Local\Temp\sillon-audit\audit-2026-04-29.md` (ce fichier).
- Aucun commit, aucun push, aucune mutation DB.
